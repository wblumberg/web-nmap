This directory contains ProtocolBuffer definitions to transport data between the backend and the frontend.

Other ProtocolBuffer definitions will be stored here so we can reference them in both the frontend and backend.
